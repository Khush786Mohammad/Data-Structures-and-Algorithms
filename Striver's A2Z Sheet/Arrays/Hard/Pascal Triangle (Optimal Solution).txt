class Solution {
	public:
	vector<int> nthRowOfPascalTriangle(int n) {
		vector<int> nums;
		nums.push_back(1);
		int ans = 1;
		
		for (int i = 1 ; i<n - 1; i++) {
			ans = ans * (n - i);
			ans = ans/i;
			nums.push_back(ans);
		}
		if (n != 1)
			nums.push_back(1);
		return nums;
	}
};
